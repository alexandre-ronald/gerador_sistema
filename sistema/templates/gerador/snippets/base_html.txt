<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ sistema.nome }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background-color: #f8f9fa; }
        
        /* Ajustes para Menu Lateral */
        {% if sistema.tipo_menu == 'lateral' %}
        .sidebar { min-width: 250px; max-width: 250px; min-height: 100vh; background: #212529; color: white; transition: all 0.3s; }
        .sidebar .nav-link { color: rgba(255,255,255,.75); border-radius: 8px; margin: 2px 10px; }
        .sidebar .nav-link:hover, .sidebar .nav-link.active { background: rgba(255,255,255,.1); color: white; }
        .main-content { width: 100%; overflow-y: auto; }
        {% endif %}

        /* Ajustes para Menu Superior */
        {% if sistema.tipo_menu == 'superior' %}
        .navbar-brand { font-weight: bold; color: #0dcaf0 !important; }
        .nav-link i { margin-right: 5px; }
        {% endif %}

        .card-summary { border: none; border-radius: 15px; transition: transform 0.2s; }
        .card-summary:hover { transform: translateY(-5px); }
    </style>
</head>
<body>
    <div class="{% if sistema.tipo_menu == 'lateral' %}d-flex{% endif %}">
        
        {% if sistema.tipo_menu == 'lateral' %}
        <nav class="sidebar shadow shadow-lg">
            <div class="p-4 text-center border-bottom border-secondary mb-3">
                <h5 class="fw-bold mb-0 text-info"><i class="bi bi-cpu-fill me-2"></i>{{ sistema.nome }}</h5>
            </div>
            <ul class="nav flex-column">
                <li class="nav-item">
                    <a href="{% templatetag openblock %} url 'index' {% templatetag closeblock %}" class="nav-link">
                        <i class="bi bi-speedometer2 me-2"></i> Dashboard
                    </a>
                </li>
                <hr class="mx-3 border-secondary">
                <div class="small text-uppercase px-4 mb-2 text-muted" style="font-size: 0.7rem;">Módulos</div>
                
                {% for modulo in sistema.modulos.all %}
                    {% for entidade in modulo.entidades.all %}
                    <li class="nav-item">
                        <a href="{% templatetag openblock %} url '{{ modulo.nome|lower }}:{{ entidade.nome|lower }}_list' {% templatetag closeblock %}" class="nav-link">
                            <i class="bi bi-circle-fill me-2" style="font-size: 0.5rem;"></i> {{ entidade.nome|title }}
                        </a>
                    </li>
                    {% endfor %}
                {% endfor %}
            </ul>
        </nav>
        {% endif %}

        <div class="main-content w-100">
            <nav class="navbar navbar-expand-lg {% if sistema.tipo_menu == 'superior' %}navbar-dark bg-dark{% else %}navbar-light bg-white border-bottom{% endif %} px-4">
                <div class="container-fluid">
                    {% if sistema.tipo_menu == 'superior' %}
                        <a class="navbar-brand" href="{% templatetag openblock %} url 'index' {% templatetag closeblock %}">
                            <i class="bi bi-cpu-fill me-2"></i>{{ sistema.nome }}
                        </a>
                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                            <span class="navbar-toggler-icon"></span>
                        </button>
                        <div class="collapse navbar-collapse" id="navbarNav">
                            <ul class="navbar-nav me-auto">
                                {% for modulo in sistema.modulos.all %}
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                        {{ modulo.nome }}
                                    </a>
                                    <ul class="dropdown-menu">
                                        {% for entidade in modulo.entidades.all %}
                                        <li><a class="dropdown-item" href="{% templatetag openblock %} url '{{ modulo.nome|lower }}:{{ entidade.nome|lower }}_list' {% templatetag closeblock %}">{{ entidade.nome|title }}</a></li>
                                        {% endfor %}
                                    </ul>
                                </li>
                                {% endfor %}
                            </ul>
                    {% else %}
                        <span class="navbar-text fw-semibold">Gerenciador de Módulos</span>
                        <div class="d-flex ms-auto">
                    {% endif %}

                        <div class="d-flex align-items-center">
                            <span class="me-3 text-muted small {% if sistema.tipo_menu == 'superior' %}text-light{% endif %}">
                                Olá, <strong>{% templatetag openvariable %} user.username {% templatetag closevariable %}</strong>
                            </span>
                            {% templatetag openblock %} if user.is_authenticated {% templatetag closeblock %}
                                <form action="{% url 'logout' %}" method="post" style="display:inline;">
                                    {% templatetag openblock %} csrf_token {% templatetag closeblock %}
                                    <button type="submit" class="btn btn-outline-danger btn-sm">Sair</button>
                                </form>
                            {% templatetag openblock %} else {% templatetag closeblock %}
                                <a href="{% url 'login' %}" class="btn btn-outline-info btn-sm">Entrar</a>
                            {% templatetag openblock %} endif {% templatetag closeblock %}
                        </div>
                    {% if sistema.tipo_menu == 'superior' %}
                        </div>
                    {% else %}
                        </div>
                    {% endif %}
                </div>
            </nav>

            <div class="p-4">
                {% templatetag openblock %} if messages {% templatetag closeblock %}
                    {% templatetag openblock %} for message in messages {% templatetag closeblock %}
                        <div class="alert alert-{% templatetag openvariable %} message.tags {% templatetag closevariable %} alert-dismissible fade show shadow-sm border-0 mb-4" role="alert">
                            {% templatetag openvariable %} message {% templatetag closevariable %}
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    {% templatetag openblock %} endfor {% templatetag closeblock %}
                {% templatetag openblock %} endif {% templatetag closeblock %}

                {% templatetag openblock %} block content {% templatetag closeblock %}
                {% templatetag openblock %} endblock {% templatetag closeblock %}
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>