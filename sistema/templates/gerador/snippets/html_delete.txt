{% templatetag openblock %} extends 'base.html' {% templatetag closeblock %}

{% templatetag openblock %} block content {% templatetag closeblock %}
<div class="row justify-content-center pt-5">
    <div class="col-md-6">
        <div class="card border-0 shadow-sm rounded-4 text-center p-4">
            <div class="text-danger mb-3"><i class="bi bi-exclamation-triangle-fill" style="font-size: 3rem;"></i></div>
            <h4 class="fw-bold">Confirmar Exclusão</h4>
            <p class="text-muted">Tem certeza que deseja remover este registro de <strong>{{ entidade.nome|title }}</strong>? Esta ação não pode ser desfeita.</p>
            <form method="post">
                {% templatetag openblock %} csrf_token {% templatetag closeblock %}
                <div class="d-grid gap-2 d-md-block mt-4">
                    <button type="submit" class="btn btn-danger px-5 me-md-2">Sim, Excluir</button>
                    <a href="{% templatetag openblock %} url '{{ app_name }}:{{ entidade.nome_tecnico }}_list' {% templatetag closeblock %}" class="btn btn-light px-5">Cancelar</a>
                </div>
            </form>
        </div>
    </div>
</div>
{% templatetag openblock %} endblock {% templatetag closeblock %}