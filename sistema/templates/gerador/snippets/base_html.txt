<!DOCTYPE html>
<html lang="pt-br" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ sistema.nome }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: "Poppins", sans-serif; font-size: .875rem; transition: background-color .3s ease, color .3s ease; }
        .sidebar { min-width: 250px; max-width: 250px; min-height: 100vh; background: #1a1d20; border-right: 1px solid rgba(255,255,255,.1); }
        .sidebar .nav-link { color: rgba(255,255,255,.7) !important; margin: 4px 12px; border-radius: 8px; }
        .sidebar .nav-link:hover, .sidebar .nav-link.active { background: rgba(255,255,255,.1); color: #0dcaf0 !important; }
        .sidebar hr { border-color: rgba(255,255,255,.1); }
        .user-chip { background: var(--bs-secondary-bg); padding: .4rem 1rem; border-radius: 20px; font-weight: 500; display: flex; align-items: center; gap: .5rem; border: 1px solid var(--bs-border-color); }
        .card { background-color: var(--bs-card-bg); border-color: var(--bs-border-color-translucent); }
        .card-header { background-color: var(--bs-tertiary-bg) !important; color: var(--bs-heading-color) !important; border-bottom: 1px solid var(--bs-border-color-translucent); }
        td, th { vertical-align: middle; }
        html[data-bs-theme="dark"] { background-color: #121212; }
        .main-content { transition: all .3s; }
    </style>
</head>
<body>
    <div class="{% if sistema.tipo_menu == 'lateral' %}d-flex{% endif %}">
        {% if sistema.tipo_menu == 'lateral' %}
        <nav class="sidebar shadow">
            <div class="p-4 border-bottom border-secondary border-opacity-25 mb-3 text-center">
                <h6 class="mb-0 text-white fw-bold">
                    <i class="bi bi-stack-overflow me-2 text-info"></i>{{ sistema.nome }}
                </h6>
            </div>
            <ul class="nav flex-column">
                <li class="nav-item">
                    <a href="{% templatetag openblock %} url 'index' {% templatetag closeblock %}" class="nav-link {% templatetag openblock %} if request.resolver_match.url_name == 'index' {% templatetag closeblock %}active{% templatetag openblock %} endif {% templatetag closeblock %}">
                        <i class="bi bi-speedometer2 me-2"></i> Dashboard
                    </a>
                </li>
                <hr class="mx-3">
                <div class="small text-uppercase px-4 mb-2 text-white-50" style="font-size:.65rem;letter-spacing:1px;">Módulos</div>
                {% for modulo in modulos %}
                    <li class="nav-item">
                        <div class="small text-uppercase px-4 mt-3 mb-1 text-white-50 fw-semibold">{{ modulo.nome }}</div>
                    </li>
                    {% for entidade in modulo.entidades.all %}
                        {% if entidade.gerar_crud_views %}
                        <li class="nav-item">
                            {% templatetag openblock %} if perms.{{ modulo.app_name }}.view_{{ entidade.nome|lower }} {% templatetag closeblock %}
                            <a href="{% templatetag openblock %} url '{{ modulo.app_name }}:{{ entidade.nome|lower }}_list' {% templatetag closeblock %}" class="nav-link {% templatetag openblock %} if request.resolver_match.app_name == '{{ modulo.app_name }}' and request.resolver_match.url_name == '{{ entidade.nome|lower }}_list' {% templatetag closeblock %}active{% templatetag openblock %} endif {% templatetag closeblock %}">
                                <i class="bi bi-layers me-2"></i>{{ entidade.nome|title }}
                            </a>
                            {% templatetag openblock %} endif {% templatetag closeblock %}
                        </li>
                        {% endif %}
                    {% endfor %}
                {% endfor %}
            </ul>
        </nav>
        {% endif %}

        <div class="main-content w-100">
            <nav class="navbar navbar-expand-lg border-bottom px-4 py-2 {% if sistema.tipo_menu == 'superior' %}navbar-dark bg-dark{% else %}bg-body-tertiary{% endif %}">
                <div class="container-fluid">
                    {% if sistema.tipo_menu == 'superior' %}
                        <a class="navbar-brand" href="{% templatetag openblock %} url 'index' {% templatetag closeblock %}">
                            <i class="bi bi-cpu-fill me-2"></i>{{ sistema.nome }}
                        </a>
                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"><span class="navbar-toggler-icon"></span></button>
                        <div class="collapse navbar-collapse" id="navbarNav">
                            <ul class="navbar-nav me-auto">
                            {% for modulo in modulos %}
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">{{ modulo.nome }}</a>
                                    <ul class="dropdown-menu shadow">
                                    {% for entidade in modulo.entidades.all %}
                                        {% if entidade.gerar_crud_views %}
                                        <li>{% templatetag openblock %} if perms.{{ modulo.app_name }}.view_{{ entidade.nome|lower }} {% templatetag closeblock %}<a class="dropdown-item {% templatetag openblock %} if request.resolver_match.app_name == '{{ modulo.app_name }}' and request.resolver_match.url_name == '{{ entidade.nome|lower }}_list' {% templatetag closeblock %}active{% templatetag openblock %} endif {% templatetag closeblock %}" href="{% templatetag openblock %} url '{{ modulo.app_name }}:{{ entidade.nome|lower }}_list' {% templatetag closeblock %}">{{ entidade.nome|title }}</a>{% templatetag openblock %} endif {% templatetag closeblock %}</li>
                                        {% endif %}
                                    {% endfor %}
                                    </ul>
                                </li>
                            {% endfor %}
                            </ul>
                    {% else %}
                        <span class="navbar-text fw-medium text-secondary"><i class="bi bi-chevron-right me-1"></i> Painel Administrativo</span>
                        <div class="ms-auto d-flex align-items-center">
                    {% endif %}

                        <div class="d-flex align-items-center gap-3">
                            <button id="theme-toggle" class="btn btn-link text-body border-0 p-0 d-flex align-items-center justify-content-center ms-2" style="width:32px;height:32px;text-decoration:none;box-shadow:none" type="button" title="Alternar Tema">
                                <i class="bi bi-moon-stars-fill" id="theme-icon" style="font-size:1.1rem"></i>
                            </button>
                            <div class="user-chip"><i class="bi bi-person-circle text-primary"></i><span class="small">{% templatetag openvariable %} user.username|default:"Visitante" {% templatetag closevariable %}</span></div>
                            {% templatetag openblock %} if user.is_authenticated {% templatetag closeblock %}
                                <form action="{% templatetag openblock %} url 'logout' {% templatetag closeblock %}" method="post" class="m-0">
                                    {% templatetag openblock %} csrf_token {% templatetag closeblock %}
                                    <button type="submit" class="btn btn-danger btn-sm px-3">Sair</button>
                                </form>
                            {% templatetag openblock %} else {% templatetag closeblock %}
                                <a href="{% templatetag openblock %} url 'login' {% templatetag closeblock %}" class="btn btn-primary btn-sm px-3">Entrar</a>
                            {% templatetag openblock %} endif {% templatetag closeblock %}
                        </div>
                    {% if sistema.tipo_menu == 'superior' %}</div>{% else %}</div>{% endif %}
                </div>
            </nav>

            <div class="p-4">
                {% templatetag openblock %} if messages {% templatetag closeblock %}
                    {% templatetag openblock %} for message in messages {% templatetag closeblock %}
                        <div class="alert alert-{% templatetag openvariable %} message.tags {% templatetag closevariable %} alert-dismissible fade show border-0 shadow-sm" role="alert">
                            {% templatetag openvariable %} message {% templatetag closevariable %}
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    {% templatetag openblock %} endfor {% templatetag closeblock %}
                {% templatetag openblock %} endif {% templatetag closeblock %}
                {% templatetag openblock %} block content {% templatetag closeblock %}{% templatetag openblock %} endblock {% templatetag closeblock %}
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const themeToggle = document.getElementById('theme-toggle');
        const themeIcon = document.getElementById('theme-icon');
        const htmlElement = document.documentElement;
        function applyTheme(theme) {
            htmlElement.setAttribute('data-bs-theme', theme);
            localStorage.setItem('theme', theme);
            if (theme === 'dark') themeIcon.classList.replace('bi-moon-stars-fill', 'bi-sun-fill');
            else themeIcon.classList.replace('bi-sun-fill', 'bi-moon-stars-fill');
        }
        applyTheme(localStorage.getItem('theme') || 'light');
        themeToggle.addEventListener('click', () => applyTheme(htmlElement.getAttribute('data-bs-theme') === 'light' ? 'dark' : 'light'));
    </script>
</body>
</html>