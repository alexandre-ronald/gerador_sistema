from django.contrib import admin
{% if entidades %}from .models import {% for entidade in entidades %}{{ entidade.classe_nome }}{% if not forloop.last %}, {% endif %}{% endfor %}{% else %}# Este módulo ainda não possui entidades.{% endif %}

{% for entidade in entidades %}{% if entidade.gerar_admin %}
@admin.register({{ entidade.classe_nome }})
class {{ entidade.classe_nome }}Admin(admin.ModelAdmin):
    list_display = [{% for campo in entidade.campos_geracao %}'{{ campo.codigo_nome }}'{% if not forloop.last %}, {% endif %}{% endfor %}]
{% endif %}{% endfor %}
