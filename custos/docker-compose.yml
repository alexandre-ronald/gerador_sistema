version: '3.8'

services:
  web:
    build: .
    container_name: app_sistema_de_custos_hospitalares
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1