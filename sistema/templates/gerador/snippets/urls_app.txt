from django.urls import path
from . import views

app_name = '{{ app_name }}'

urlpatterns = [
    {% for entidade in entidades %}
    # Rotas para {{ entidade.nome|title }}
    path('{{ entidade.nome|lower }}/', views.{{ entidade.nome|title }}ListView.as_view(), name='{{ entidade.nome|lower }}_list'),
    path('{{ entidade.nome|lower }}/novo/', views.{{ entidade.nome|title }}CreateView.as_view(), name='{{ entidade.nome|lower }}_create'),
    path('{{ entidade.nome|lower }}/<int:pk>/editar/', views.{{ entidade.nome|title }}UpdateView.as_view(), name='{{ entidade.nome|lower }}_update'),
    path('{{ entidade.nome|lower }}/<int:pk>/deletar/', views.{{ entidade.nome|title }}DeleteView.as_view(), name='{{ entidade.nome|lower }}_delete'),
    {% endfor %}
]