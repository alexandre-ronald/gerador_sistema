{% templatetag openblock %} extends 'base.html' {% templatetag closeblock %}
{% templatetag openblock %} block content {% templatetag closeblock %}
<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4">
  <div>
    <div class="text-uppercase small fw-bold text-primary mb-1">{{ entidade.crud_title }}</div>
    <h1 class="h3 fw-bold mb-1">Detalhes do registro</h1>
    <p class="text-secondary mb-0">Visualização segura dos campos configurados na listagem.</p>
  </div>
  <div class="d-flex gap-2">
    <a href="{% templatetag openblock %} url '{{ app_name }}:{{ entidade.codigo_nome }}_list' {% templatetag closeblock %}" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Voltar</a>
{% if entidade.crud_actions.edit %}{% templatetag openblock %} if perms.{{ app_name }}.change_{{ entidade.codigo_nome }} {% templatetag closeblock %}<a href="{% templatetag openblock %} url '{{ app_name }}:{{ entidade.codigo_nome }}_update' objeto.pk {% templatetag closeblock %}" class="btn btn-primary"><i class="bi bi-pencil me-1"></i>Editar</a>{% templatetag openblock %} endif {% templatetag closeblock %}{% endif %}
  </div>
</div>
<div class="app-card"><div class="p-4"><div class="row g-4">{% for coluna in entidade.crud_visible_columns %}<div class="col-md-6"><div class="small text-uppercase fw-bold text-secondary mb-1">{{ coluna.label }}</div><div class="fs-6">{% templatetag openvariable %} objeto.{{ coluna.codigo_nome }} {% templatetag closevariable %}</div></div>{% endfor %}</div></div></div>
{% templatetag openblock %} endblock {% templatetag closeblock %}
