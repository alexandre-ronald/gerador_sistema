<!DOCTYPE html>
<html lang="pt-br" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ sistema.nome }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { font-family: Poppins, sans-serif; font-size: .875rem; }
        .sidebar { min-width: 250px; max-width: 250px; min-height: 100vh; background: #1a1d20; }
        .sidebar .nav-link { color: rgba(255,255,255,.7) !important; margin: 4px 12px; border-radius: 8px; }
        .sidebar .nav-link:hover, .sidebar .nav-link.active { background: rgba(255,255,255,.1); color: #0dcaf0 !important; }
        .top-module-link { white-space: nowrap; }
        .user-chip { background: var(--bs-secondary-bg); padding: .4rem 1rem; border-radius: 20px; border: 1px solid var(--bs-border-color); }
    </style>
</head>
<body>
<!-- Runtime contract: app navigation | module permissions | authentication | CSRF | content block -->
<div class="{% if sistema.tipo_menu == 'lateral' %}d-flex{% endif %}">
{% if sistema.tipo_menu == 'lateral' %}
<nav class="sidebar shadow">
    <div class="p-4 border-bottom border-secondary border-opacity-25 mb-3 text-center">
        <h6 class="mb-0 text-white fw-bold"><i class="bi bi-stack-overflow me-2 text-info"></i>{{ sistema.nome }}</h6>
    </div>
    <ul class="nav flex-column">
        <li class="nav-item">
            <a href="{% templatetag openblock %} url 'index' {% templatetag closeblock %}" class="nav-link {% templatetag openblock %} if request.resolver_match.url_name == 'index' {% templatetag closeblock %}active{% templatetag openblock %} endif {% templatetag closeblock %}">
                <i class="bi bi-speedometer2 me-2"></i>Dashboard
            </a>
        </li>
        <hr class="mx-3">
        <div class="small text-uppercase px-4 mb-2 text-white-50">Módulos</div>
        {% for modulo in modulos %}
        <li class="nav-item">
            <a href="#modulo-{{ modulo.app_name }}" class="nav-link {% templatetag openblock %} if request.resolver_match.app_name == '{{ modulo.app_name }}' {% templatetag closeblock %}active{% templatetag openblock %} endif {% templatetag closeblock %}">
                <i class="bi bi-grid me-2"></i>{{ modulo.nome }}
            </a>
        </li>
        <li id="modulo-{{ modulo.app_name }}" class="nav-item">
            <div class="small text-uppercase px-4 mt-2 mb-1 text-white-50 fw-semibold">{{ modulo.nome }}</div>
        </li>
        {% for entidade in modulo.entidades.all %}
        {% if entidade.gerar_crud_views %}
        <li class="nav-item">
            {% templatetag openblock %} if perms.{{ modulo.app_name }}.view_{{ entidade.codigo_nome }} {% templatetag closeblock %}
            <a href="{% templatetag openblock %} url '{{ modulo.app_name }}:{{ entidade.codigo_nome }}_list' {% templatetag closeblock %}" class="nav-link {% templatetag openblock %} if request.resolver_match.app_name == '{{ modulo.app_name }}' {% templatetag closeblock %}active{% templatetag openblock %} endif {% templatetag closeblock %}">
                <i class="bi bi-layers me-2"></i>{{ entidade.nome }}
            </a>
            {% templatetag openblock %} endif {% templatetag closeblock %}
        </li>
        {% endif %}
        {% endfor %}
        {% endfor %}
    </ul>
</nav>
{% endif %}

<div class="main-content w-100">
<nav class="navbar navbar-expand-lg border-bottom px-4 py-2 bg-body-tertiary">
    <div class="container-fluid">
        <span class="navbar-text fw-medium text-secondary"><i class="bi bi-chevron-right me-1"></i>Painel Administrativo</span>
        {% if sistema.tipo_menu == 'superior' %}
        <div class="d-flex align-items-center gap-2 ms-3 overflow-auto">
            <a href="{% templatetag openblock %} url 'index' {% templatetag closeblock %}" class="btn btn-sm btn-outline-secondary top-module-link">Dashboard</a>
            {% for modulo in modulos %}
                {% for entidade in modulo.entidades.all %}
                    {% if entidade.gerar_crud_views %}
                    {% templatetag openblock %} if perms.{{ modulo.app_name }}.view_{{ entidade.codigo_nome }} {% templatetag closeblock %}
                    <a href="{% templatetag openblock %} url '{{ modulo.app_name }}:{{ entidade.codigo_nome }}_list' {% templatetag closeblock %}" class="btn btn-sm btn-outline-secondary top-module-link">{{ entidade.nome }}</a>
                    {% templatetag openblock %} endif {% templatetag closeblock %}
                    {% endif %}
                {% endfor %}
            {% endfor %}
        </div>
        {% endif %}
        <div class="ms-auto d-flex align-items-center gap-3">
            <div class="user-chip"><i class="bi bi-person-circle text-primary"></i> <span class="small">{% templatetag openvariable %} user.username|default:"Visitante" {% templatetag closevariable %}</span></div>
            {% templatetag openblock %} if user.is_authenticated {% templatetag closeblock %}
            <form action="{% templatetag openblock %} url 'logout' {% templatetag closeblock %}" method="post" class="m-0">
                {% templatetag openblock %} csrf_token {% templatetag closeblock %}
                <button type="submit" class="btn btn-danger btn-sm">Sair</button>
            </form>
            {% templatetag openblock %} else {% templatetag closeblock %}
            <a href="{% templatetag openblock %} url 'login' {% templatetag closeblock %}" class="btn btn-primary btn-sm">Entrar</a>
            {% templatetag openblock %} endif {% templatetag closeblock %}
        </div>
    </div>
</nav>
<div class="p-4">
    {% templatetag openblock %} if messages {% templatetag closeblock %}
    {% templatetag openblock %} for message in messages {% templatetag closeblock %}
    <div class="alert alert-{% templatetag openvariable %} message.tags {% templatetag closevariable %}">{{ message }}</div>
    {% templatetag openblock %} endfor {% templatetag closeblock %}
    {% templatetag openblock %} endif {% templatetag closeblock %}
    {% templatetag openblock %} block content {% templatetag closeblock %}{% templatetag openblock %} endblock {% templatetag closeblock %}
</div>
</div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
