{% extends 'base.html' %}

{% block title %}Início | {{ sistema.nome }}{% endblock %}

{% block content %}
<div class="container py-4">
  <div class="p-4 p-lg-5 rounded-4 border bg-body shadow-sm">
    <div class="small text-uppercase fw-bold text-primary mb-2">Aplicação gerada pelo DjangoForge</div>
    <h1 class="fw-bold mb-3">{{ sistema.nome }}</h1>
    <p class="text-secondary mb-4">{{ sistema.descricao|default:'Sistema gerado com sucesso e pronto para evolução.' }}</p>

    <div class="row g-3">
      {% for modulo in modulos %}
      <div class="col-md-6 col-xl-4">
        <div class="card h-100 border-0 shadow-sm">
          <div class="card-body">
            <h5 class="card-title fw-bold">{{ modulo.nome }}</h5>
            <p class="text-secondary small mb-0">Acesse as funcionalidades disponíveis no menu de navegação.</p>
          </div>
        </div>
      </div>
      {% empty %}
      <div class="col-12">
        <div class="alert alert-light border mb-0">Nenhum módulo configurado.</div>
      </div>
      {% endfor %}
    </div>
  </div>
</div>
{% endblock %}
