version: '3.8'

services:
  web:
    build: .
    container_name: app_{{ nome_projeto }}
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DJANGO_DEBUG=1
      - DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,web
{% if sistema.banco_dados == 'postgresql' %}
      - POSTGRES_DB={{ nome_projeto }}_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
    depends_on:
      - db

  db:
    image: postgres:16-alpine
    container_name: db_{{ nome_projeto }}
    environment:
      - POSTGRES_DB={{ nome_projeto }}_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data_{{ nome_projeto }}:/var/lib/postgresql/data

volumes:
  postgres_data_{{ nome_projeto }}:
{% elif sistema.banco_dados == 'mysql' %}
      - MYSQL_DATABASE={{ nome_projeto }}_db
      - MYSQL_USER=app
      - MYSQL_PASSWORD=app
      - MYSQL_HOST=db
      - MYSQL_PORT=3306
    depends_on:
      - db

  db:
    image: mysql:8.0
    container_name: db_{{ nome_projeto }}
    environment:
      - MYSQL_DATABASE={{ nome_projeto }}_db
      - MYSQL_USER=app
      - MYSQL_PASSWORD=app
      - MYSQL_ROOT_PASSWORD=root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data_{{ nome_projeto }}:/var/lib/mysql

volumes:
  mysql_data_{{ nome_projeto }}:
{% else %}
    # SQLite (ou outro banco sem serviço dedicado neste compose)
{% endif %}
