{% templatetag openblock %} extends 'base.html' {% templatetag closeblock %}
{% templatetag openblock %} block content {% templatetag closeblock %}
<div class="container-fluid py-4">
    <div class="row mb-5"><div class="col-12 text-center text-md-start">
        <h1 class="welcome-header fw-bold">Bem-vindo ao {{ sistema.nome }} <i class="bi bi-rocket-takeoff-fill rocket-icon"></i></h1>
        <p class="text-muted fs-5">Gestão simplificada. O que vamos acessar agora?</p>
    </div></div>

    <h5 class="fw-bold mb-4"><i class="bi bi-grid-fill text-primary"></i> Módulos Ativos</h5>
    <div class="row g-4 mb-5">
        {% for modulo in modulos %}
        <div class="col-md-6 col-lg-3"><div class="card dashboard-card p-4 h-100">
            <div class="rounded-circle bg-primary bg-opacity-10 p-3 mb-3 d-flex align-items-center justify-content-center" style="width:50px;height:50px;"><i class="bi bi-folder2-open text-primary fs-4"></i></div>
            <h4 class="fw-bold mb-1">{{ modulo.nome }}</h4>
            <p class="text-muted small mb-0">{{ modulo.entidades_geracao|length }} Tabelas registradas</p>
        </div></div>
        {% empty %}<div class="col-12"><div class="alert alert-info">Nenhum módulo cadastrado.</div></div>{% endfor %}
    </div>

    <h5 class="fw-bold mb-4"><i class="bi bi-layers-half text-info"></i> Entidades do Sistema</h5>
    <div class="row g-3">
        {% for modulo in modulos %}
            {% for entidade in modulo.entidades_geracao %}
                {% if entidade.gerar_crud_views %}
                <div class="col-md-4 col-lg-3">
                    <a href="{% templatetag openblock %} url '{{ modulo.app_name }}:{{ entidade.codigo_nome }}_list' {% templatetag closeblock %}" class="entity-card border shadow-sm">
                        <div class="d-flex align-items-center justify-content-between"><div class="d-flex align-items-center gap-3">
                            <div class="entity-icon-wrapper"><i class="bi bi-table"></i></div>
                            <div><div class="module-badge text-primary">{{ modulo.nome }}</div><div class="fw-bold fs-6">{{ entidade.nome|title }}</div></div>
                        </div><i class="bi bi-arrow-right-short fs-4 opacity-50"></i></div>
                    </a>
                </div>
                {% endif %}
            {% endfor %}
        {% empty %}<div class="col-12"><div class="alert alert-info">Nenhuma entidade cadastrada.</div></div>{% endfor %}
    </div>
</div>
{% templatetag openblock %} endblock {% templatetag closeblock %}