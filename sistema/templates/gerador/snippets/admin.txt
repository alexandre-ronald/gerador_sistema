from django.contrib import admin

from .models import {% for entidade in entidades %}{{ entidade.classe_tecnica }}{% if not forloop.last %}, {% endif %}{% endfor %}

{% for entidade in entidades %}
@admin.register({{ entidade.classe_tecnica }})
class {{ entidade.classe_tecnica }}Admin(admin.ModelAdmin):
    list_display = [{% for campo in entidade.campos.all %}"{{ campo.nome_tecnico }}", {% endfor %}]
{% endfor %}
